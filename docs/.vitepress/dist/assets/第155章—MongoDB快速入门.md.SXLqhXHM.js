import{_ as s,c as a,o as n,V as p}from"./chunks/framework.BbAbY5cn.js";const e="/assets/第155章-1.l72iVvxf.png",t="/assets/第155章-2.uJchz7vo.png",i="/assets/第155章-3.OPxtuuFM.png",l="/assets/第155章-4.AwERa4_a.png",o="/assets/第155章-5.EZaeEZrW.png",c="/assets/第155章-6.H2Zsz8K1.png",d="/assets/第155章-7.3FfM2jmu.png",g="/assets/第155章-8.jRCFl9DS.png",r="/assets/第155章-9.n05fLUe1.png",m="/assets/第155章-10.l2GIUlHY.png",b="/assets/第155章-11.sbNz6o-C.png",h="/assets/第155章-12.dyhIqMeG.png",u="/assets/第155章-13.B08EIewP.png",x="/assets/第155章-14.tpfN7z7j.png",_="/assets/第155章-15.D4si-Qk8.png",v="/assets/第155章-16.5sWRdgD3.png",k="/assets/第155章-17.jhL7RALM.png",y="/assets/第155章-18.lXypGxyE.png",C="/assets/第155章-19.jAqOaKE_.png",f="/assets/第155章-20._wNSEVWG.png",$="/assets/第155章-21.zmLUvmxJ.png",O="/assets/第155章-22.5joT4mxQ.png",w="/assets/第155章-23.4_txU6r1.png",q="/assets/第155章-24.SAC9JqeO.png",D="/assets/第155章-25.AWbChs7N.png",M="/assets/第155章-26.Cx7iXwWo.png",U="/assets/第155章-27.TW25y196.png",j="/assets/第155章-28.TXfFls3U.png",I="/assets/第155章-29.jfWPKJtp.png",R="/assets/第155章-30.2xOLmY1d.png",A="/assets/第155章-31.gZjRtvU8.png",B="/assets/第155章-32.RwAbelpR.png",E="/assets/第155章-33.xBWpej7i.png",T="/assets/第155章-34.fJjQkdQI.png",G="/assets/第155章-35.yu51LBF8.png",K=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"第155章—MongoDB快速入门.md","filePath":"第155章—MongoDB快速入门.md"}'),P={name:"第155章—MongoDB快速入门.md"},S=p('<p>数据库我们学习了 mysql，这节来再来学下 mongodb。</p><p>在 docker desktop 搜索 mongo 镜像，然后把它跑起来：</p><p><img src="'+e+'" alt=""></p><p>指定容器名、映射的端口号，以及挂载到 /data/db 的本地目录：</p><p><img src="'+t+'" alt=""></p><p>这个本地目录是本地的任何一个目录都行，用来存放 mongodb 的数据的。</p><p><img src="'+i+'" alt=""></p><p>跑起来之后，进入 terminal，开始学习 mongodb 的 crud：</p><p><img src="'+l+`" alt=""></p><p>输入 mongosh，就可以敲 mongo 命令了：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>show dbs;</span></span>
<span class="line"><span>show databases;</span></span></code></pre></div><p><img src="`+o+'" alt=""></p><p>查看下现有的 database。</p><p>和 mysql 一样，mongodb 也是通过一个个 database 来存储不同数据的，只不过 mysql 的 database 里存的是 table，而 mongo 里存的是 Collection。</p><p><img src="'+c+'" alt=""></p><p>创建或者切换 database 用 use</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>use hello-mongo;</span></span></code></pre></div><p>然后 db 命令查看当前 database：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>db;</span></span></code></pre></div><p><img src="'+d+'" alt=""></p><p>但这时候你再用 show dbs 会发现没有这个 db：</p><p><img src="'+g+'" alt=""></p><p>这是因为默认有了一个 collection 之后才会把 database 写入硬盘。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>db.createCollection(&#39;aaa&#39;);</span></span></code></pre></div><p>创建了一个 collection 之后，再次 show dbs，这时候就看到这个新的 database 了：</p><p><img src="'+r+'" alt=""></p><p>如果你想删除 database，用这个命令：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>db.dropDatabase();</span></span></code></pre></div><p><img src="'+m+'" alt=""></p><p>创建和删除 database 我们会了，然后是 collection：</p><p><img src="'+b+`" alt=""></p><p>用 db.createCollection 创建 db，然后可以用 db.xxx.drop() 删掉。</p><p>接下来向 collection 中插入几个 document：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>db.xxx.insertOne({ name: &#39;guang&#39;, age: 20, phone: &#39;13222222222&#39;});</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.insertMany([{ name: &#39;dong&#39;, age: 21}, {name: &#39;xxx&#39;, hobbies: [&#39;writing&#39;]}]);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.insertMany([{ name: &#39;dong2&#39;, age: 20}, {name: &#39;guang2&#39;, hobbies: [&#39;writing&#39;]}]);</span></span></code></pre></div><p><img src="`+h+'" alt=""></p><p>可以看到，插入的数据可以是任意结构的，这就是 mongodb 和 mysql 的最大区别。</p><p>不像 mysql 只能在 table 里存结构化的记录一样，mongodb 的 collection 里可以存任意结构的 document。</p><p>再就是 mysql 是需要用 sql 来 crud 的，而 mongodb 里是用 api。</p><p>但完成的功能是一样的，可以在 <a href="https://www.mongodb.com/docs/mongodb-shell/crud/" target="_blank" rel="noreferrer">mongodb 文档</a>里看到每个 api 的介绍下面都有对应的 sql 写法：</p><p><img src="'+u+'" alt=""></p><p><img src="'+x+`" alt=""></p><p>我们试下查询：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>db.xxx.find({age: 20});</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.findOne({ age: 20});</span></span></code></pre></div><p><img src="`+_+'" alt=""></p><p>find 是查询多条记录，而 findOne 只返回第一条。</p><p>当不传过滤条件的时候，就是返回所有的 Document：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>db.xxx.find()</span></span></code></pre></div><p><img src="'+v+'" alt=""></p><p>查询条件有多个值的时候这样写，类似 sql 的 in：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>db.xxx.find({ age: { $in: [20, 21]}})</span></span></code></pre></div><p><img src="'+k+'" alt=""></p><p>in 是包含， nin 是不包含。</p><p>类似 in 的还有 gt（great than）、lt（less than）、gte（great than equal ）、lte（less than equal ）、ne（not equal）</p><p><img src="'+y+`" alt=""></p><p>多个条件的时候用 and：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>db.xxx.find({ $and: [{age: { $gte: 20 }}, { name: /dong\\d/}]})</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.find({ $and: [{age: { $gte: 20 }}, { name: /dong*/}]})</span></span></code></pre></div><p>搜索 age 大于等于 20，并且名字包含 dong 的 Document，这里用的正则做的模糊匹配：</p><p><img src="`+C+'" alt=""></p><p>用 or 的话就是两个条件满足一个就行：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>db.xxx.find({ $or: [{age: { $gt: 20 }}, { name: /dong*/}]})</span></span></code></pre></div><p><img src="'+f+'" alt=""></p><p>分页用的是 skip 和 limit 方法：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>db.xxx.find().skip(1).limit(2)</span></span></code></pre></div><p>从第一条 Document 开始，取 2 条：</p><p><img src="'+$+'" alt=""></p><p>更新的话用 updateOne、updateMany 方法：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>db.xxx.updateOne({ name: &#39;guang&#39;}, { $set: {age: 30} })</span></span></code></pre></div><p><img src="'+O+'" alt=""></p><p>如果在更新的时候带上时间戳，可以用 $currentDate：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>db.xxx.updateOne({ name: &#39;guang&#39;}, { $set: {age: 30}, $currentDate: { aaa: true } })</span></span></code></pre></div><p><img src="'+w+'" alt=""></p><p>如果你想替换整个的 document，那么用 replaceOne：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>db.xxx.replaceOne({name: &#39;guang&#39;}, { age: 30})</span></span></code></pre></div><p><img src="'+q+'" alt=""></p><p>删除的话，也有 deleteOne 和 deleteMany 两个方法：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>db.xxx.deleteMany({ age: { $gt: 20 }});</span></span></code></pre></div><p><img src="'+D+`" alt=""></p><p>此外，还有 count 可以计数：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>db.xxx.count()</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.count({ name: /guang/})</span></span></code></pre></div><p><img src="`+M+'" alt=""></p><p>sort 可以排序，1 是升序、-1 是降序：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>db.xxx.find().sort({ age: -1})</span></span></code></pre></div><p>按照 age 降序：</p><p><img src="'+U+'" alt=""></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>db.xxx.find().sort({ age: -1, name: 1})</span></span></code></pre></div><p>先按照 age 降序，再按照 name 升序：</p><p><img src="'+j+'" alt=""></p><p>这样，CRUD 我们都过了一遍。</p><p>当然，这些操作也都可以在 GUI 里可视化的搞，这样就不用敲命令了：</p><p>下载官方 GUI 工具 <a href="https://www.mongodb.com/products/tools/compass" target="_blank" rel="noreferrer">Mongo Compass</a>：</p><p><img src="'+I+'" alt=""></p><p>连接上 mongodb 的 server：</p><p><img src="'+R+'" alt=""></p><p>在 GUI 工具里操作就很方便直观了：</p><p><img src="'+A+'" alt=""></p><p>可以看到所有的 database、collection、document。</p><p>在这里输入过滤条件后点击 find：</p><p><img src="'+B+'" alt=""></p><p>更新和删除也都很直观：</p><p><img src="'+E+'" alt=""></p><p><img src="'+T+'" alt=""></p><p><img src="'+G+`" alt=""></p><h2 id="总结" tabindex="-1">总结 <a class="header-anchor" href="#总结" aria-label="Permalink to &quot;总结&quot;">​</a></h2><p>这节我们入门了 mongodb。</p><p>它的 database 下是 collection，collection 包含多个 document。</p><p>document 可以是任意结构的，这点和 mysql 的 table 不同。</p><p>mongo 是通过 api 来做 CRUD 的，不是用的 sql。</p><p>我们过了一遍常用的命令和 api：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>show dbs; // 显示 database</span></span>
<span class="line"><span>show databases;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>use hello-mongo; // 切换或者创建 database</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db; // 查看当前 database</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.dropDatabase(); // 删除 database</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.createCollection(&#39;aaa&#39;); // 创建 collection</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.drop() //删除 collection</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.insertOne({ name: &#39;guang&#39;, age: 20, phone: &#39;13222222222&#39;});// 插入 document</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.insertMany([{ name: &#39;dong&#39;, age: 21}, {name: &#39;xxx&#39;, hobbies: [&#39;writing&#39;]}]);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.find({age: 20}); // 查询 collection</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.findOne({ age: 20});</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.findOne(); // 查询所有 collection</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.find({ age: { $in: [20, 21]}}) // 使用 in、nin 查询多个值</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.find({ $and: [{age: { $gte: 20 }}, { name: /dong\\d/}]}) // 使用 and 指定多个条件同时成立</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.find({ $or: [{age: { $gt: 20 }}, { name: /dong*/}]}) // 使用 or 表示或者</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.find().skip(1).limit(2) // 使用 skip + limit 实现分页</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.updateOne({ name: &#39;guang&#39;}, { $set: {age: 30}, $currentDate: { aaa: true } }) // updateOne、updateMany 更新</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.replaceOne({name: &#39;guang&#39;}, { age: 30}) // 整体替换</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.deleteMany({ age: { $gt: 20 }}); // deleteOne、deleteMany 删除</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.count({ name: /guang/})</span></span>
<span class="line"><span></span></span>
<span class="line"><span>db.xxx.find().sort({ age: -1, name: 1})</span></span></code></pre></div><p>当然，平时不用敲 api，用 Mongo Compass 的 GUI 工具操作非常简单。</p>`,110),W=[S];function N(J,L,V,z,F,Q){return n(),a("div",null,W)}const X=s(P,[["render",N]]);export{K as __pageData,X as default};
